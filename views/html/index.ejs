<!DOCTYPE html>
<html lang="en">
<head>
    <%-include("./partials/head.ejs")%>
</head>
<body>
    <header>
        <%-include("./partials/header.ejs")%>
    </header>
    <div class="d-flex justify-content-center">
        <form action="/api/short" method="POST" class="d-flex justify-content-center">
            <div>
                <label for="longUrl" class="form-laber">Input Your URL</label>
                <input type="text" name="longUrl" id="longUrl" required>
                <button type="submit" class="btn btn-primary">Generate URL</button>
            </div>
    </div>    
        </form>
    <footer>
        <%-include("./partials/footer.ejs")%>
    </footer>
</body>
</html>